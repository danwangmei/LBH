1 块级作用域
1.1 允许块级作用域的任意嵌套
1.2 内层作用域可以定义外层作用域的同名变量
1.3 避免在块级作用域中声明函数,实在需要,也应该写成函数表达式的形式,而不是函数声明语句
// 函数声明语句
{
	let a = 'secret';
	function f() {
		return	a;
	} 
}
// 函数表达式
{
	let a = 'secret';
	let f = function () {
		return	a;
	}
}